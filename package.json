{
  "name": "nativebpm-sdk-typescript-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "NativeBPM TypeScript SDK & Zod Schema Monorepo",
  "type": "module",
  "workspaces": [
    "typescript"
  ],
  "scripts": {
    "build": "npm run build --workspace=@nativebpm/sdk",
    "test": "npm run test --workspace=@nativebpm/sdk"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
